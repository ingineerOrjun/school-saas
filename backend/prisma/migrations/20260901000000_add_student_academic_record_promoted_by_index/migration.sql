-- ============================================================================
-- Session 5 — heal `student_academic_records_promotedById_idx` divergence.
--
-- Background: migration 20260715000000_promotion_safety_publish_state
-- creates this index at the SQL level with CREATE INDEX IF NOT EXISTS,
-- but the schema.prisma model never declared a matching
-- `@@index([promotedById])`. Over time, that mismatch let the dev DB
-- get into a state where the index was dropped (likely by the
-- introspection-driven DROP that ended up in
-- 20260513182148_add_learning_outcomes). Schema and dev DB ended up
-- agreeing on "no index" while migration history said "have index" —
-- a structural drift `prisma migrate dev` refused to bridge until the
-- schema was updated to match the history.
--
-- Session 5 closes the loop:
--   • schema.prisma now declares `@@index([promotedById])` on
--     StudentAcademicRecord (the rightful source of truth).
--   • This migration creates the index for any dev DB that's missing
--     it. IF NOT EXISTS keeps the statement idempotent on:
--       (a) production DBs where 0715 already created the index, AND
--       (b) any dev DB that, somehow, retained the index past 0513.
--
-- Body generated by:
--   npx prisma migrate diff
--     --from-schema-datasource prisma/schema.prisma
--     --to-schema-datamodel    prisma/schema.prisma
--     --script
-- with `IF NOT EXISTS` added by hand because Prisma 6.19.3's diff
-- emits a bare `CREATE INDEX`. Prefer this defensive form for any
-- index whose existence varies across deployment targets.
-- ============================================================================

-- CreateIndex
CREATE INDEX IF NOT EXISTS "student_academic_records_promotedById_idx" ON "student_academic_records"("promotedById");
